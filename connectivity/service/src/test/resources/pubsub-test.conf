pubsub {
  consumer {
    init-timeout-seconds = 3
  }
  producer {
    init-timeout-seconds = 3
  }
}

pekko.connectors.googlecloud.pubsub {
  project-id = "my-local-project"
  project-id = ${?PUBSUB_PROJECT_ID}
  host = "localhost"
  port = 8085
  port = ${?PUBSUB_EMULATOR_PORT}
}

pekko.connectors.google {
  project-id = "my-local-project"
  project-id = ${?PUBSUB_PROJECT_ID}
  credentials {
    provider = "none"
    none {
      project-id = "my-local-project"
      project-id = ${?PUBSUB_PROJECT_ID}
      token = "none"
    }
    transport = "none"
  }
}
